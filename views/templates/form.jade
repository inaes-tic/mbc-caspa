block content
      mixin field(type, id, label, value)
        .fieldset(id=id, class='field-' + type)
          label #{label}
          .fieldwrap
            input(type=type, name=id, value=value)
    .span4
      mixin field('text', 'TodoInput', 'Create a new Todo', '')
      .btn.button#add Add